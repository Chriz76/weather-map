// Central provider ID constants to avoid magic strings and circular imports
export const D2 = 'icon-d2';
export const AROME = 'arome';

export const PROVIDER_IDS = Object.freeze({ D2, AROME });

export default PROVIDER_IDS;
